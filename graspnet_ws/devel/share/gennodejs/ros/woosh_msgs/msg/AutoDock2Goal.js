// Auto-generated. Do not edit!

// (in-package woosh_msgs.msg)


"use strict";

const _serializer = _ros_msg_utils.Serialize;
const _arraySerializer = _serializer.Array;
const _deserializer = _ros_msg_utils.Deserialize;
const _arrayDeserializer = _deserializer.Array;
const _finder = _ros_msg_utils.Find;
const _getByteLength = _ros_msg_utils.getByteLength;
let geometry_msgs = _finder('geometry_msgs');

//-----------------------------------------------------------

class AutoDock2Goal {
  constructor(initObj={}) {
    if (initObj === null) {
      // initObj === null is a special case for deserialization where we don't initialize fields
      this.tag_no = null;
      this.board_shape = null;
      this.type = null;
      this.dock_direction = null;
      this.targetPose = null;
    }
    else {
      if (initObj.hasOwnProperty('tag_no')) {
        this.tag_no = initObj.tag_no
      }
      else {
        this.tag_no = '';
      }
      if (initObj.hasOwnProperty('board_shape')) {
        this.board_shape = initObj.board_shape
      }
      else {
        this.board_shape = 0;
      }
      if (initObj.hasOwnProperty('type')) {
        this.type = initObj.type
      }
      else {
        this.type = 0;
      }
      if (initObj.hasOwnProperty('dock_direction')) {
        this.dock_direction = initObj.dock_direction
      }
      else {
        this.dock_direction = 0;
      }
      if (initObj.hasOwnProperty('targetPose')) {
        this.targetPose = initObj.targetPose
      }
      else {
        this.targetPose = new geometry_msgs.msg.Pose2D();
      }
    }
  }

  static serialize(obj, buffer, bufferOffset) {
    // Serializes a message object of type AutoDock2Goal
    // Serialize message field [tag_no]
    bufferOffset = _serializer.string(obj.tag_no, buffer, bufferOffset);
    // Serialize message field [board_shape]
    bufferOffset = _serializer.int8(obj.board_shape, buffer, bufferOffset);
    // Serialize message field [type]
    bufferOffset = _serializer.int8(obj.type, buffer, bufferOffset);
    // Serialize message field [dock_direction]
    bufferOffset = _serializer.int8(obj.dock_direction, buffer, bufferOffset);
    // Serialize message field [targetPose]
    bufferOffset = geometry_msgs.msg.Pose2D.serialize(obj.targetPose, buffer, bufferOffset);
    return bufferOffset;
  }

  static deserialize(buffer, bufferOffset=[0]) {
    //deserializes a message object of type AutoDock2Goal
    let len;
    let data = new AutoDock2Goal(null);
    // Deserialize message field [tag_no]
    data.tag_no = _deserializer.string(buffer, bufferOffset);
    // Deserialize message field [board_shape]
    data.board_shape = _deserializer.int8(buffer, bufferOffset);
    // Deserialize message field [type]
    data.type = _deserializer.int8(buffer, bufferOffset);
    // Deserialize message field [dock_direction]
    data.dock_direction = _deserializer.int8(buffer, bufferOffset);
    // Deserialize message field [targetPose]
    data.targetPose = geometry_msgs.msg.Pose2D.deserialize(buffer, bufferOffset);
    return data;
  }

  static getMessageSize(object) {
    let length = 0;
    length += _getByteLength(object.tag_no);
    return length + 31;
  }

  static datatype() {
    // Returns string type for a message object
    return 'woosh_msgs/AutoDock2Goal';
  }

  static md5sum() {
    //Returns md5sum for a message object
    return '59093fc5b352f7e2936d701a4c288571';
  }

  static messageDefinition() {
    // Returns full string definition for message
    return `
    # ====== DO NOT MODIFY! AUTOGENERATED FROM AN ACTION DEFINITION ======
    string  tag_no                      #tag编号
    
    int8    board_shape
    int8    ALL_SHAPE = 0               #操作所有特征板
    int8    VL_SHAPE = 1                #VL型特征板
    int8    PARALLEL_SHAPE = 2          #平行特征板
    int8    CONVEX_SHAPE = 3            #凸性特征板
    int8    NARROW_PASSAGE_SHAPE = 4    #狭窄通道特征形状
    int8    ELEVATOR_SHAPE = 5          #电梯口特征形状
    int8    RECTANGLE_SHAPE = 6            #料车
    
    int8    type
    int8    ENTER = 0
    int8    PAUSE = 1
    int8    RESUM = 2
    int8    LEAVE = 3
    int8    RESET = 4
    
    int8    dock_direction              #对接方向
    int8    BACKWARD = 0
    int8    FRONTWARD = 1
    
    geometry_msgs/Pose2D  targetPose    #当此接口用于狭窄通道的时候，此变量起作用。用于判断狭窄通道出口位置点。
    
    ================================================================================
    MSG: geometry_msgs/Pose2D
    # Deprecated
    # Please use the full 3D pose.
    
    # In general our recommendation is to use a full 3D representation of everything and for 2D specific applications make the appropriate projections into the plane for their calculations but optimally will preserve the 3D information during processing.
    
    # If we have parallel copies of 2D datatypes every UI and other pipeline will end up needing to have dual interfaces to plot everything. And you will end up with not being able to use 3D tools for 2D use cases even if they're completely valid, as you'd have to reimplement it with different inputs and outputs. It's not particularly hard to plot the 2D pose or compute the yaw error for the Pose message and there are already tools and libraries that can do this for you.
    
    
    # This expresses a position and orientation on a 2D manifold.
    
    float64 x
    float64 y
    float64 theta
    
    `;
  }

  static Resolve(msg) {
    // deep-construct a valid message object instance of whatever was passed in
    if (typeof msg !== 'object' || msg === null) {
      msg = {};
    }
    const resolved = new AutoDock2Goal(null);
    if (msg.tag_no !== undefined) {
      resolved.tag_no = msg.tag_no;
    }
    else {
      resolved.tag_no = ''
    }

    if (msg.board_shape !== undefined) {
      resolved.board_shape = msg.board_shape;
    }
    else {
      resolved.board_shape = 0
    }

    if (msg.type !== undefined) {
      resolved.type = msg.type;
    }
    else {
      resolved.type = 0
    }

    if (msg.dock_direction !== undefined) {
      resolved.dock_direction = msg.dock_direction;
    }
    else {
      resolved.dock_direction = 0
    }

    if (msg.targetPose !== undefined) {
      resolved.targetPose = geometry_msgs.msg.Pose2D.Resolve(msg.targetPose)
    }
    else {
      resolved.targetPose = new geometry_msgs.msg.Pose2D()
    }

    return resolved;
    }
};

// Constants for message
AutoDock2Goal.Constants = {
  ALL_SHAPE: 0,
  VL_SHAPE: 1,
  PARALLEL_SHAPE: 2,
  CONVEX_SHAPE: 3,
  NARROW_PASSAGE_SHAPE: 4,
  ELEVATOR_SHAPE: 5,
  RECTANGLE_SHAPE: 6,
  ENTER: 0,
  PAUSE: 1,
  RESUM: 2,
  LEAVE: 3,
  RESET: 4,
  BACKWARD: 0,
  FRONTWARD: 1,
}

module.exports = AutoDock2Goal;
